name: ithelpdesk
description: A new Flutter project.
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 4.0.4+1

environment:
  sdk: '>=3.0.6 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins: 
    sdk: flutter       


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  hive_flutter: ^1.1.0
  get_it: ^8.2.0
  dio: ^5.9.0
  equatable: ^2.0.7
  connectivity_plus: ^6.1.5
  flutter_localizations:
    sdk: flutter
  intl: any
  url_launcher: ^6.3.2
  mime: ^2.0.0
  geolocator: ^14.0.2
  dartz: ^0.10.1
  flutter_svg: ^2.2.1
  page_transition: ^2.2.1
  flutter_bloc: ^9.1.1
  flutter_phoenix: 1.1.1
  path_provider: ^2.1.5
  rxdart: ^0.28.0
  audioplayers: ^6.5.1
  file_picker: ^10.1.2
  image_picker: ^1.1.2
  flutter_local_notifications: ^16.3.0
  device_info_plus: ^11.5.0
  file_saver: ^0.3.1
  flutter_dotenv: ^6.0.0
  encrypt: ^5.0.3
  firebase_analytics: ^12.0.1
  firebase_crashlytics: ^5.0.1
  firebase_core: ^4.1.0
  firebase_messaging: ^16.0.1
  package_info_plus: ^8.3.1
  animated_sidebar: ^1.0.1
  fl_chart: ^1.1.0
  syncfusion_flutter_xlsio: ^31.1.17
  syncfusion_flutter_pdfviewer: ^31.1.17
  dropdown_search: ^6.0.2
  flutter_typeahead: ^5.2.0
  go_router: ^16.2.1
  web: ^1.1.1
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  generate: true

  # To add assets to your application, add an assets section, like this:
  assets:
  # app files:
  # app audios:
  # app images gif:
  # app images jpg:
  # app images png:
     - assets/images/ic_logo.png
     - assets/images/ic_sgd_logo.png
  # app images svg:
     - assets/html/print.html
     - assets/images/ic_logo.svg
     - assets/images/ic_home.svg
     - assets/images/ic_report.svg
     - assets/images/ic_report.png
     - assets/images/ic_directory.svg
     - assets/images/ic_user.svg
     - assets/images/ic_notification.svg
     - assets/images/ic_search.svg
     - assets/images/ic_not_assigned_requests.svg
     - assets/images/ic_user_circle.svg
     - assets/images/ic_checkmark_circle.svg
     - assets/images/ic_open_requests.svg
     - assets/images/ic_closed_requests.svg
     - assets/images/ic_no._of_requests.svg
     - assets/images/ic_plus_circle.svg
     - assets/images/ic_close_circle.svg
     - assets/images/ic_cross.svg
     - assets/images/ic_message.svg
     - assets/images/ic_view.svg
     - assets/images/ic_delete.svg
     - assets/images/ic_lang_en.svg
     - assets/images/ic_lang_ar.svg
     - assets/images/ic_chevron_down.svg
     - assets/images/ic_upload.svg
     - assets/images/ic_attachment.svg
     - assets/images/ic_star.svg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: Inter
      fonts:
        - asset: fonts/inter_regular.otf
        - asset: fonts/inter_regular.otf
          weight: 400
        - asset: fonts/inter_semibold.otf
          weight: 600
        - asset: fonts/inter_semibold.otf
          weight: 700
        - asset: fonts/inter_bold.otf
          weight: 900
    - family: Poppins
      fonts:
        - asset: fonts/Poppins-Regular.ttf
        - asset: fonts/Poppins-Light.ttf
          weight: 300
        - asset: fonts/Poppins-Regular.ttf
          weight: 400
        - asset: fonts/Poppins-Medium.ttf
          weight: 500
        - asset: fonts/Poppins-SemiBold.ttf
          weight: 600
        - asset: fonts/Poppins-Bold.ttf
          weight: 700
        - asset: fonts/Poppins-Bold.ttf
          weight: 800
        - asset: fonts/Poppins-Bold.ttf
          weight: 900
    - family: AR_GE_SS
      fonts:
        - asset: fonts/ar_ge_ss_two_medium.otf
        - asset: fonts/ar_ge_ss_two_light.otf
          weight: 400
        - asset: fonts/ar_ge_ss_two_medium.otf
          weight: 600
        - asset: fonts/ar_ge_ss_two_bold.otf
          weight: 700
        - asset: fonts/ar_ge_ss_two_bold.otf
          weight: 900
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
